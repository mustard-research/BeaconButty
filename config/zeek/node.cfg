# Zeek node configuration — standalone sensor
#
# "standalone" mode runs all Zeek components (logger, manager, worker)
# in a single process, which is appropriate for a Raspberry Pi.
#
# __CAPTURE_IFACE__ is replaced by setup script with the actual interface name.

[zeek]
type=standalone
host=localhost
interface=__CAPTURE_IFACE__
